using UnityEngine;
using UnityEngine.UI;
using TMPro;

public class ShopItemRow : MonoBehaviour
{/* TODO later
    public Item item;
    //public Image icon;
    public GameObject soldOutOverlay;
    public Button buyButton;

    public void Setup(Item newItem, Shop shop)
    {
        item = newItem;
        //icon.sprite = item.icon;

        buyButton.onClick.RemoveAllListeners();
        buyButton.onClick.AddListener(() => shop.BuyWeapon(item));

        soldOutOverlay.SetActive(false);
    }

    public void SetSoldOut()
    {
        soldOutOverlay.SetActive(true);
        buyButton.interactable = false;
    }*/
}